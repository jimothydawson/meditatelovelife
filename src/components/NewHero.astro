---
/**
 * Redesigned Hero Section
 * Image: zoltan-tasi-vHnVtLK8rCc-unsplash.jpg
 */
---

<section class="hero">
    <!-- Background Image with Overlay -->
    <div class="hero-bg-image">
        <img
            src="/images/zoltan-tasi-vHnVtLK8rCc-unsplash.jpg"
            alt="Vedic Meditation Calm Lotus"
        />
        <div class="hero-overlay"></div>
    </div>

    <!-- Animated Orbs for Depth -->
    <div class="hero-bg-orb one"></div>
    <div class="hero-bg-orb two"></div>
    <div class="hero-bg-orb three"></div>

    <div class="hero-content">
        <p class="hero-eyebrow">Vedic Meditation · Sydney</p>
        <h1>Less Pressure.<br /><em>More Calm.</em></h1>
        <p class="hero-sub">
            A simple, natural way to dissolve stress at its source and restore
            your true nature.
        </p>
        <a href="#book" class="btn btn--primary hero-cta"
            >Learn Vedic Meditation</a
        >
    </div>

    <div class="hero-scroll">
        <div class="hero-scroll-line"></div>
        <span>Scroll</span>
    </div>
</section>

<style>
    .hero {
        position: relative;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: var(--color-indigo);
        overflow: hidden;
    }

    .hero-bg-image {
        position: absolute;
        inset: 0;
        z-index: 0;
    }

    .hero-bg-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        object-position: center;
        opacity: 0.6; /* Blending the image with the background colors */
    }

    .hero-overlay {
        position: absolute;
        inset: 0;
        background: linear-gradient(
            165deg,
            rgba(31, 47, 58, 0.8) 0%,
            rgba(31, 47, 58, 0.4) 40%,
            rgba(31, 47, 58, 0.8) 100%
        );
    }

    .hero-bg-orb {
        position: absolute;
        border-radius: 50%;
        filter: blur(100px);
        animation: breathe 8s ease-in-out infinite;
        z-index: 1;
    }

    .hero-bg-orb.one {
        width: 600px;
        height: 600px;
        background: radial-gradient(
            circle,
            rgba(92, 127, 120, 0.25),
            transparent
        );
        top: -10%;
        left: -5%;
    }

    .hero-bg-orb.two {
        width: 500px;
        height: 500px;
        background: radial-gradient(
            circle,
            rgba(245, 200, 71, 0.12),
            transparent
        );
        bottom: -15%;
        right: 10%;
        animation-delay: 3s;
    }

    .hero-bg-orb.three {
        width: 300px;
        height: 300px;
        background: radial-gradient(
            circle,
            rgba(104, 138, 133, 0.2),
            transparent
        );
        top: 30%;
        right: -5%;
        animation-delay: 5s;
    }

    .hero-content {
        position: relative;
        z-index: 2;
        text-align: center;
        max-width: 780px;
        padding: 0 32px;
    }

    .hero-eyebrow {
        font-size: 12px;
        letter-spacing: 4px;
        text-transform: uppercase;
        color: var(--color-eucalyptus);
        margin-bottom: 40px;
        animation: fadeIn 1s ease 0.3s both;
    }

    .hero h1 {
        font-size: clamp(42px, 6.5vw, 80px);
        color: var(--color-warm-white);
        margin-bottom: 32px;
        animation: fadeUp 1.2s ease 0.5s both;
        letter-spacing: -0.5px;
    }

    .hero h1 em {
        font-style: italic;
        color: var(--color-golden-seed);
    }

    .hero-sub {
        font-size: clamp(17px, 2vw, 20px);
        color: rgba(244, 242, 238, 0.7);
        max-width: 520px;
        margin: 0 auto 48px;
        line-height: 1.8;
        animation: fadeUp 1.2s ease 0.8s both;
    }

    .hero-cta {
        animation: fadeUp 1.2s ease 1.1s both;
    }

    .hero-scroll {
        position: absolute;
        bottom: 40px;
        left: 50%;
        transform: translateX(-50%);
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 8px;
        color: rgba(244, 242, 238, 0.35);
        font-size: 11px;
        letter-spacing: 2px;
        text-transform: uppercase;
        animation: fadeIn 1s ease 1.8s both;
        z-index: 2;
    }

    .hero-scroll-line {
        width: 1px;
        height: 40px;
        background: linear-gradient(
            to bottom,
            rgba(244, 242, 238, 0.3),
            transparent
        );
        animation: gentleFloat 3s ease-in-out infinite;
    }
</style>
